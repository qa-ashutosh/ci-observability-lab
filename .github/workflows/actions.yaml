# Event name
name: CI

# When the workflow should run
on:
  push:
    branches:
      - "**"  # Run on all branches

# What jobs to execute
jobs:
  build_and_test:
    runs-on: ubuntu-latest

    # What steps to execute within the job
    steps:

      # Set a start time
      - name: Set start time
        run: echo "START_TIME=$(date +%s)" >> "$GITHUB_ENV"

      # Checkout repository
      - uses: actions/checkout@v4

      # Install Java
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: maven

      # Run tests with Maven
      - name: Run tests with Maven
        run: mvn test

      # Calculate total duration and collect test data
      - name: Collect build data
        if: always()
        run: |
      # Calculate total duration and collect test data
      - name: Collect build data
        if: always()
        run: |
          # Calculate total duration
          TOTAL_DURATION=$(( $(date +%s) - START_TIME ))

          # Push duration metric to Prometheus Pushgateway
          echo "github_actions_total_duration ${TOTAL_DURATION}" | \
            curl --fail --silent --show-error \
            --data-binary @- "${{ secrets.PUSHGATEWAY_URL }}/metrics/job/github_actions" || \
            echo "WARNING: Failed to push duration metric to Pushgateway"

          # Extract test results
          bash ./src/test/resources/test_result_script.sh